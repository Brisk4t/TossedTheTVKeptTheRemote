Web Serial config utility

Open index.html in a browser with Web Serial support.
Use Connect, then Load From Device.
Select a button on the grid, click Learn IR, and press the remote button.
Edit the Key/Usage value, then Apply (RAM) or Apply + Save.

The JSON panel is still available for advanced edits.
